version: '3.8'

services:
  dbcserver:
    image: dbcserver:latest
    container_name: dbcserver
    network_mode: host
    cap_add:
      - NET_RAW
    volumes:
      - ./output/server.conf:/app/server.conf:ro
      - ./logs:/app/logs
    restart: unless-stopped
